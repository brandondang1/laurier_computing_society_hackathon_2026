Install instructions,

Frontend:
npm install <br />
npm run dev

Backend:
npm install <br />
npm start

For this project we used the React framework, tailwind for CSS styling, react-konva for the canvas and drawing graphics, Lucide react for the icon libraries. <br />
Backend we used Node.js and socket io for the client connections.
